import { MediaPRStrategyPage } from "@/components/capabilities/media-pr-strategy-page";

export const metadata = {
  title: "Media, PR & Strategy",
  description: "WALLS media, PR, and strategy capabilities for creator brand building.",
};

export default function MediaPRStrategyRoute() {
  return <MediaPRStrategyPage />;
}
