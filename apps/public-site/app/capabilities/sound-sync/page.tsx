import { SoundSyncPage } from "@/components/capabilities/sound-sync-page";

export const metadata = {
  title: "Sound Sync",
  description: "WALLS Sound Sync — music licensing and sync capabilities for creators and brands.",
};

export default function SoundSyncRoute() {
  return <SoundSyncPage />;
}
